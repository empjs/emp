{
    "name": "emp3-vue2-demo",
    "ignored": true,
    "version": "0.0.1",
    "scripts": {
      "dev": "pnpm --filter vue-2-* --parallel dev",
      "build": "pnpm --filter vue-2-* --parallel build",
      "serve": "pnpm --filter vue-2-* --parallel serve",
      "stat": "pnpm --filter vue-2-* --parallel stat"
    }
  }
  
{
    "name": "emp3-vue2-demo",
    "ignored": true,
    "version": "0.0.1",
    "scripts": {
      "dev": "pnpm --filter 'vue2-*' --parallel dev",
      "build": "pnpm --filter vue2-* --parallel build",
      "serve": "pnpm --filter vue2-* --parallel serve",
      "stat": "pnpm --filter vue2-* --parallel stat"
    }
  }
  
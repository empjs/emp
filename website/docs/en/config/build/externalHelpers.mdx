# build.externalHelpers
+ SWC imports some helper classes as modules
+ Type: `boolean`
+ Default: `false`

```ts title="emp-config.js"
import {defineConfig} from '@empjs/cli'
export default defineConfig(store => {
  return {
    build:{
        externalHelpers:false,
    }
  }
})
```
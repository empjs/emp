# chain
+ Overrides `rspack` configuration
## Configuration
```ts title="emp-config.js"
import {defineConfig} from '@empjs/cli'
export default defineConfig(store => {
    // 方法一
    store.chain.module.rule('javascript').test(/\.js$/)
  return {
    chain(chain){
        // 方法二
        chain.module.rule('javascript').test(/\.js$/)
    },
  }
})
```
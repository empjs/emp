# define
+ Global environment replacement
+ Type: `Record<string, any>`

## defineFix
+ Type `all` | `esm` | `cjs` | `none`
+ Default `cjs`

:::tip 
Whether to create process.env for cjs or import.meta.env for esm
all creates both
esm creates import.meta.env
cjs creates process.env
none creates neither
:::

```ts title="emp-config.js"
import {defineConfig} from '@empjs/cli'
export default defineConfig(store => {
  return {
        define:{
            cdnHost:'cdn.com'
        },
        defineFix:'cjs'
    }
  }
})
```
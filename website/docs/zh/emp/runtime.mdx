# @empjs/share/runtime
> EMP 运行时引用

## 参考用例 
+ [Module Federation consumer 验收用例](https://github.com/empjs/emp/blob/main/apps/mf-app/src/App.tsx)
+ [Module Federation remote 验收用例](https://github.com/empjs/emp/blob/main/apps/mf-host/src/App.tsx)
+ [Rslib/CDN runtime smoke](https://github.com/empjs/emp/blob/main/scripts/library-output.smoke.test.ts)
+ [引用 @module-federation/enhanced/runtime](https://module-federation.io/zh/guide/basic/runtime/runtime-api.html#runtime-api)
## 项目调用

```js
import {reactAdapter} from '@empjs/share/adapter'
import empRuntime from '@empjs/share/runtime'
import {lazy, Suspense, useEffect} from 'react'

const ip = process.env.ip
empRuntime.init({
  shared: reactAdapter.shared,
  remotes: [
    {
      name: 'react_astro_ui',
      entry: `http://${ip}:2800/emp.json`,
    },
  ],
})
export const App = () => {
  const Layout = lazy(() => empRuntime.load('react_astro_ui/App'))
  useEffect(() => {
    setTimeout(async () => {
      // console.log('react_astro_ui', (await empRuntime.load('react_astro_ui/App')).default.toString())
    }, 1000)
  }, [])
  return (
    <Suspense fallback="loading...">
      <Layout>
        <h1>react 18</h1>
      </Layout>
    </Suspense>
  )
}
export default App

```
